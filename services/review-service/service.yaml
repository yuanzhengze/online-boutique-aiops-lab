apiVersion: v1
kind: Service
metadata:
  name: review-service
  labels:
    app: review-service
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 32180
  selector:
    app: review-service
